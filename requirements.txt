# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
pytorch-lightning>=2.0.0
torchmetrics>=0.11.0

# Data handling
numpy>=1.24.0
scipy>=1.10.0
h5py>=3.8.0
pillow>=9.5.0

# Logging and experiment tracking
wandb>=0.15.0
huggingface-hub>=0.20.0  # For model uploads/downloads

# Model architectures
timm>=0.9.0

# LoRA fine-tuning (pulls in transformers, accelerate, etc.)
peft>=0.4.0
